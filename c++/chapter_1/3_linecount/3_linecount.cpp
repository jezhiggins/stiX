//
// linecount
//
// linecount counts the lines in its input and writes the total
// as line of text to the output
//

#include "linecount.hpp"
#include <iostream>

int main() {
    std::cout << stiX::linecount(std::cin) << std::endl;
}
